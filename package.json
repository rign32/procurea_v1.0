{
  "devDependencies": {
    "firebase-tools": "^15.5.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "frontend/node_modules/.bin/eslint --fix --no-error-on-unmatched-pattern --config frontend/eslint.config.js"
    ]
  }
}
